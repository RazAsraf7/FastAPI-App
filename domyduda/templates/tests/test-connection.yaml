apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "domyduda.fullname" . }}-test-connection"
  labels:
    {{- include "domyduda.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "domyduda.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
