apiVersion: v1
kind: Service
metadata:
  name: {{ include "domyduda.fullname" . }}
  labels:
    {{- include "domyduda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "domyduda.selectorLabels" . | nindent 4 }}
